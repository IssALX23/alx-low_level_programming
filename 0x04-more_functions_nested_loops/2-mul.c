#include "main.h"
#include <stdio.h>
#include "main.h"
/**
 * mul - writes the multiple of two integers
 * @a: input integer
 * @b: input integer
 *
 * Return: On success 0.
*/
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
