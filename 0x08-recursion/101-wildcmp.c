#include "main.h"
/**
 * is_palindrome - function that compares two strings and returns 1 if the strings can
 * be considered identical, otherwise return 0
 * @s1: pointer to string
 * @s2: pointer to string
 *
 * Return: integer
 */
int wildcmp(char *s1, char *s2)
{
}
